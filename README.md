# DTM Worklog Prototype

Cloudflare Pages 向けの Vite プロジェクトです。

## ローカル実行

```bash
npm install
npm run dev
```

## Cloudflare Pages

React (Vite) の標準設定で使えます。

- Build command: `npm run build`
- Build output directory: `dist`

GitHub 連携で push ごとに自動デプロイできます。
